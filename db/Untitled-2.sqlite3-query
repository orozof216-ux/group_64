-- database: ./todo.db

SELECT * FROM tasks;

    CREATE TABLE IF NOT EXISTS test (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        number INTEGER NOT NULL
    );

SELECT * FROM test; 

INSERT INTO test (name, number) VALUES (321, 0703217081);

UPDATE test SET number = 070321 WHERE id = 1;

select * from test WHERE name = 'Нурислам';

ALTER TABLE test ADD COLUMN completed INTEGER DEFAULT 0;